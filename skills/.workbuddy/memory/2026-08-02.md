# 2026-08-02

## devops-skill 分析并安装到 WorkBuddy

源目录：`/Users/patrick/innate/innate-works/skills/devops-skill`（git repo，origin = github.com/qdriven/devops-skill，main 分支）

### 分析结论
- 是一个含 8 个子技能的合集：git-workflow、local-workflow、git-worktree、github-cli-skill(name: github-cli)、git-pr、gh-create-release、scanning-for-secrets、docmd
- 所有 SKILL.md 的 `metadata.supported_agents` 已声明 `workbuddy`，但原安装脚本并不支持该目标
- 安全审查通过：无网络下载 / 无 `curl|bash` / 不读凭据；`rm -rf` 仅限自身安装目标；`sudo` 仅在交互确认安装 gh 时触发；`git commit/push` 仅在显式 `--commit/--push` 时执行

### 发现的两个缺陷
1. 安装脚本 `SKILLS` 数组漏了 `scanning-for-secrets`（README 声称 8 个，实际只装 7 个）
2. SKILL.md / 脚本里硬编码 `.agents/skills/` 与 `~/.claude/skills/` 路径，装到 WorkBuddy 后命令路径失效

### 所做修改（未 commit，未 push）
- `dev-workflow-install.sh`：新增 `--agent workbuddy` → `${WORKBUDDY_HOME:-$HOME/.workbuddy}/skills`；加入默认 system 目标列表；补上 `scanning-for-secrets`；新增 `rewrite_workbuddy_paths()`，WorkBuddy 目标走**实体复制而非 symlink**并改写路径（其他 agent 仍是 symlink 到 `~/.skill-spark/.agents/skills`）
- `README.md`：补充 workbuddy 安装说明
- 未修改 `dev-workflow-install.ps1` 和 `dev-workflow-symlink-install.sh`（symlink 方式与路径改写冲突，不适用于 workbuddy）

### 重装命令
```bash
bash /Users/patrick/innate/innate-works/skills/devops-skill/dev-workflow-install.sh --system --agent workbuddy
```

### 环境事实
- WorkBuddy 用户级技能目录：`~/.workbuddy/skills/`；格式 = SKILL.md（frontmatter name/description）+ 可选子目录
- 本机 gh 2.72.0 已安装

## 15:39 重装 + 完整安装检查（devops-skill → WorkBuddy）

重跑 `dev-workflow-install.sh --system --agent workbuddy`，8 个技能全部 REPLACE 成功。验证结论：
- 8/8 SKILL.md 存在，name 字段正确（github-cli, gh-create-release, git-workflow, git-worktree, git-pr, local-workflow, docmd, scanning-for-secrets）
- 旧路径 `.agents/skills` / `.claude/skills` 零残留；`~/.workbuddy/skills/...` 路径改写已生效
- 全部 `supported_agents` 含 `workbuddy`（YAML 列表格式）
- Python 脚本 `compileall` 通过；git-workflow/git-pr/local-workflow 的 orchestrate.py --help 正常
- `gh auth status`：已登录 github.com 账号 qdriven（keyring）
- ⚠️ 无法从对话内重启 WorkBuddy 桌面应用，需用户手动重启/重载才能扫描到新技能
