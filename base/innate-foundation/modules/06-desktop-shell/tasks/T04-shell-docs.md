# 任务：编写「如何用 shell lib 新建 Desktop 应用」文档

- **状态**：pending
- **模块**：06-desktop-shell
- **优先级**：P1
- **依赖**：T03

## 目标

新人按文档 15 分钟内理解如何基于 lib 开新应用（含 IPC 约定与打包注意点）。

## 执行步骤

1. 写 `docs/create-desktop-app.md`
2. 链到 empty-app 模板
3. 列出与 `07-desktop-components`、`05-agent-runtime` 的集成接点（可先占位）

## 产出

- `docs/create-desktop-app.md`

## 验收标准

- [ ] 步骤编号完整：创建 → 依赖 lib → 跑通 → 打包注意
- [ ] 有「不要复制 flock-ui」的明确警告

## 如何执行

```text
请执行 base/innate-foundation/modules/06-desktop-shell/tasks/T04-shell-docs.md，使用 Local Workflow。
```
