# 任务：提供基于 shell lib 的空 Desktop 应用模板

- **状态**：pending
- **模块**：06-desktop-shell
- **优先级**：P0
- **依赖**：T02

## 目标

`examples/empty-app`（或 `templates/empty-app`）能 `tauri dev` 打开空白窗口，证明 lib 可消费。

## 执行步骤

1. 创建空应用模板依赖 shell lib
2. 写启动脚本与最小前端页
3. 在 README 写运行命令

## 产出

- 空应用模板
- 运行说明

## 验收标准

- [ ] 文档中的命令可启动（环境具备 Tauri 时）
- [ ] 模板不含业务页面

## 如何执行

```text
请执行 base/innate-foundation/modules/06-desktop-shell/tasks/T03-empty-app-template.md，使用 Local Workflow。
```
