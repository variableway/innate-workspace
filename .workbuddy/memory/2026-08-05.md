# 2026-08-05

## Agent Kanban Phase 3: GitHub 同步 + Taskfile

执行了双后端计划的 Phase 3（GitHub 同步），同时用 Taskfile 替代了 Makefile。

### Taskfile 替代 Makefile

为 Go 和 Node.js 后端分别创建了 Taskfile.yml，并在项目根目录创建了根 Taskfile：
- `backend-go/Taskfile.yml` — dev/build/run/test/tidy/fmt/lint/build-linux/clean/migrate-up/migrate-down
- `backend-node/Taskfile.yml` — install/dev/build/start/typecheck/test/clean
- `Taskfile.yml` (根) — build (并行双后端) / dev:go / dev:node / test / clean

验证：`task -d ... build` 两个后端都通过。

### Phase 3a: Go GitHub 同步

新增文件：
- `backend-go/internal/github/webhook.go` — HMAC-SHA256 验签 + webhook payload 解析 + HandleWebhook 事件分发
- `backend-go/internal/github/sync.go` — SyncIssue (upsert/close/label/assignee) + FullSync (分页拉取所有 issue)
- `backend-go/internal/store/sync.go` — FindProjectByRepo / UpsertTask / CloseTask / UpdateTaskLabels / UpdateTaskAssignee / UpdateProjectSyncStatus / FindMatchingAgent (Label Router) / SetAssignmentModel

更新文件：
- `handler.go` — ReceiveGitHubWebhook 完整实现 (验签 + 事件分发) + TriggerSync (后台全量同步) + AutoAssign (Label Router 完整实现)

Go 后端功能现在完整：
- ✅ HMAC-SHA256 webhook 验签
- ✅ Issue opened/closed/reopened/edited/labeled/assigned 事件同步
- ✅ 全量同步 (GitHub API 分页拉取)
- ✅ Label Router (capability_tags ∩ labels 交集匹配)
- ✅ 自动分配 (含幂等检查 + model_used 回写)

### Phase 3b: Node.js GitHub 同步

新增文件：
- `backend-node/src/lib/github/verify-signature.ts` — HMAC-SHA256 验签 (timing-safe)
- `backend-node/src/lib/github/sync.ts` — syncIssue + fullSync (fetch API 分页) + findMatchingAgent

更新文件：
- `src/routes/webhooks.ts` — 完整实现 (验签 + issue 同步)
- `src/routes/projects.ts` — TriggerSync 调用 fullSync (后台非阻塞)

Node.js 后端功能现在完整：
- ✅ HMAC-SHA256 webhook 验签 (timingSafeEqual)
- ✅ Issue opened/closed/reopened/edited/labeled/assigned 事件同步
- ✅ 全量同步 (fetch API 分页拉取)
- ✅ Label Router (已在 Phase 2 实现)
- ✅ 自动分配 (已在 Phase 2 实现)

### 两个后端功能对齐

| 功能 | Go | Node.js |
|------|-----|---------|
| HMAC 验签 | ✅ crypto/hmac | ✅ crypto.timingSafeEqual |
| Issue 同步 | ✅ | ✅ |
| 全量同步 | ✅ net/http 分页 | ✅ fetch 分页 |
| Label Router | ✅ (Phase 3 新增) | ✅ (Phase 2 已有) |
| 后台非阻塞同步 | ✅ goroutine | ✅ Promise (非 await) |

### 验证

- `go build ./...` ✅
- `tsc --noEmit` ✅
- `task -d backend-go build` ✅
- `task -d backend-node typecheck` ✅
- `task -d . build` (根 Taskfile 并行构建双后端) ✅

### 下一步
- Phase 4: 看板 UI (复用 innate-fe-base)
- Phase 6: 对比测试 + 部署
