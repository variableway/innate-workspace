# 2026-08-18

- 用 vYtDL 下载 YouTube 视频（The Cure - Pictures Of You, ltvqlQl77NE）成功，360p mp4（425s, 27M），存于 innative-works/projects/innate-keepthem/vYtDL-standalone/downloads/downloads/。
- 排障结论（vYtDL 在本机的可复用经验）：
  1. 该仓库构建需 `GOWORK=off go build`（受 go.work 影响）。
  2. WorkBuddy shell 注入的会话代理（127.0.0.1:5xxxx，端口每次会话不同）对 YouTube 报 `Tunnel connection failed: 502 Bad Gateway`；改用 macOS 系统代理（`scutil --proxy` 查，如 127.0.0.1:4780）。先用 `curl -m 10 -x <proxy> https://www.youtube.com` 验证。
  3. 系统 yt-dlp 2026.03.17 太旧且受 PEP 668 限制无法升级；已在 venv `~/.workbuddy/binaries/python/envs/default` 装 2026.07.04（pip 需指定 `-i https://pypi.org/simple/`，tuna 镜像 403）。vYtDL 用 `--yt-dlp-bin` 指向它。
  4. YouTube 视频流 403（n-challenge）：本机无 deno，换播放器客户端 `--extractor-args "youtube:player_client=web,android"` 即可（已验证，无需装 deno）。
- 以上两条环境排障已回填到 vytdl-cli skill 常见问题（~/.workbuddy/skills/vytdl-cli/SKILL.md 第 126-127 行），后续以 skill 为准。
